repos:
  - repo: local
    hooks:
      - id: repo-hygiene
        name: repo hygiene
        entry: python -B -m pycodeagent.dev.repo_hygiene check
        language: system
        pass_filenames: false
